{
  "name": "my-app-next",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev-frontend": "npm --prefix ./next run dev",
    "dev-backend": "npm --prefix ./express run dev",
    "dev": "concurrently \"npm run dev-frontend\" \"npm run dev-backend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
